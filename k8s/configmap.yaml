apiVersion: v1
kind: ConfigMap
metadata:
  name: detection-engine-config
data:
  # Loki stack in monitoring namespace (default Helm install)
  LOKI_URL: "http://loki-stack-loki.monitoring.svc.cluster.local:3100"
